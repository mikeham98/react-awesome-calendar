import {getDate, getTimeFromDate} from '../../../app/Calendar/util/date';

describe('date', () => {
    describe('getDate', () => {
        it('should return a date without the time', () => {
            // const date = new Date(2019, 0, 1, 5, 10, 42, 5);
            expect(1+1).toBe(2);
        });
    });
    describe('getTimeFromDate', () => {

    });
});